# Kiro Configuration

This directory contains Kiro-specific configuration files for this project.

## Structure

- `steering/` - Steering files for additional context and instructions
- `settings/` - Configuration files (e.g., mcp.json for MCP servers)
